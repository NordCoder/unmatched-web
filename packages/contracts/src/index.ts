/**
 * Public transport-contract package boundary.
 *
 * No command, event, state, or projection types are hand-written here. A
 * follow-up ADR will select the versioned schema source and generators.
 */
export const contractGenerationState = "not-configured" as const;
